server:
  error:
    include-message: always
    include-binding-errors: always
  port: 8081

spring:
  output:
    ansi:
      enabled: ALWAYS

application:
  loanLimitPerDay: 30
  maxAmount: 100
  forbiddenHourFrom: 0
  forbiddenHourTo: 6