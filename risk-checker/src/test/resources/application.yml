application:
  loanLimitPerDay: 1
  maxAmount: 100
  forbiddenHourFrom: 0
  forbiddenHourTo: 6

back-office:
  url: ${wiremock.server.baseUrl:http://localhost:8080}

spring:
  web:
    error:
      include-message: always
      include-binding-errors: always