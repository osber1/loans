server:
  error:
    include-message: always
    include-binding-errors: always

application:
  loanLimitPerDay: 1
  maxAmount: 100
  forbiddenHourFrom: 0
  forbiddenHourTo: 6

back-office:
  url: http://localhost:8080